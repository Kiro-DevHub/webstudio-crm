VITE_API_URL=/api

# "Log in as demo" button on /login. It signs in as the seeded MANAGER (see prisma/seed.ts).
# Leave both empty to hide the button entirely — do that anywhere that is not a local demo.
VITE_DEMO_EMAIL=olga@crm.dev
VITE_DEMO_PASSWORD=Demo1234!
