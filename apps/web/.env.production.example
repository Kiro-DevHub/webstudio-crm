# Copy to .env.production for a local prod-mode build (`vite build --mode production`),
# or set VITE_API_URL as an environment variable in the Vercel project instead.
VITE_API_URL=https://api.your-domain.com/api

# Demo button: leave both empty in production unless you want a public demo login.
VITE_DEMO_EMAIL=olga@crm.dev
VITE_DEMO_PASSWORD=Demo1234!
